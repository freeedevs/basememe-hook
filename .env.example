# Deployment environment variables (example)
#
# Copy to `.env` and fill values:
#   cp .env.example .env
#
# Then export them (one option):
#   set -a && source .env && set +a

# Deployer EOA private key (hex, with or without 0x)
PRIVATE_KEY=

# JSON-RPC URL for the target chain
RPC_URL=

# Uniswap v4 PoolManager address on the target chain
POOL_MANAGER=

# Uniswap v4 periphery PositionManager address on the target chain
POSITION_MANAGER=

# Permit2 address on the target chain
PERMIT2=

# WETH address on the target chain
WETH=

# Basememe factory address (allowed to call initializePool / setTokenGraduated)
FACTORY=

# ProtocolRewards address (deploy via DeployProtocolRewards.s.sol, or set to an existing instance)
PROTOCOL_REWARDS=

# Deployed BasememeHook address (used when deploying the LP manager)
HOOK=
